{
  "name": "translate-web-by-chatgpt-browser",
  "version": "0.2.0",
  "private": true,
  "description": "Self-contained Manifest V3 extension that translates visible English text nodes through a signed-in ChatGPT tab.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "eslint .",
    "test": "node --test",
    "check": "npm run build && npm run lint && npm test",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "@eslint/js": "9.32.0",
    "archiver": "7.0.1",
    "esbuild": "0.25.8",
    "eslint": "9.32.0",
    "globals": "16.3.0"
  }
}
